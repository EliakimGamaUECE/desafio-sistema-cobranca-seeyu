PORT=3000
WEBHOOK_SECRET=dev-secret
BILLING_BATCH_SIZE=100
MAIL_MODE=mock            # mock | ethereal | smtp (default: auto)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_RATE_DELAY_MS=350

# Proteção opcional do job/endpoint de billing
CRON_TOKEN=dev-cron-token
# Se quiser rodar o cron contra outro host
BILLING_RUN_URL=http://127.0.0.1:3000/billing/run
